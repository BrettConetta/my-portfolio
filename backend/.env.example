# Production (Railway backend service). Copy to `.env` for local contact form testing.
# FRONTEND_URL=https://your-frontend.up.railway.app

# Where contact form submissions are delivered
CONTACT_TO=brettconetta76@gmail.com

# Gmail SMTP — use your Gmail address and a Google App Password (not your normal password)
SMTP_USER=brettconetta76@gmail.com
SMTP_PASS=your-16-character-app-password

# Optional Gmail defaults (omit unless you use another provider)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
